apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sagan-pvc
  namespace: sagan-app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi 
  storageClassName: standard-rwo 